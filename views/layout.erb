<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Projekt #2</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css?<%=Time.now.to_i%>">
    <script src="/js/add_css.js?<%=Time.now.to_i%>"></script>
</head>
<body>
  <%if !header_hidden?%>
    <header>
      <nav>
        <div><a href='/'>GD-Tracker</a></div>
        <div><a href='/leaderboard'>Leaderboard</a></div>
        <div><a href='/levels'>Levels</a></div>
        <div><a href='/collections'>Collections</a></div>
      </nav></div>
      <%if session[:user]%>
        <span id="profile-link"><a href='/users/<%=session[:user][:id]%>'><%=session[:user][:name]%>#<%=session[:user][:id]%></a></span>
        <span><a href='/users/logout?redirect=<%=request.fullpath%>'>Log out</a></span>
      <%else%>
        <span><a href='/users/login?redirect=<%=request.fullpath%>'>Log in</a></span>
      <%end%>
    </header>
  <%end%>
  <main>
  <%=yield%>
  </main>
</body>
</html>